package main

import (
	"testing"
)

func TestReader(t *testing.T) {
	// r := strings.NewReader("{\"data\": {\"a\": 10}}")
	// dat := DecodeJSONBody(r)
	// if dat.Data["a"] != 10 {
	// 	t.Error("Bad read")
	// }
}
